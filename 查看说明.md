# 迷你世界 API 权限查询指南

> 本文档说明如何通过 `DevApiCfg.lua` 判断一个接口是否可以直接调用。

---

## 一、核心概念

迷你世界的 API 分为两层：

| 层级 | 字段 | 含义 |
|------|------|------|
| **引擎内部层** | `services` | 引擎实际拥有的所有方法（包含内部接口） |
| **开发者可见层** | `devServices` | 开发者可以正常调用的 API 列表 |

---

## 二、判断规则

### 规则 1：在 `devServices` 中 → ✅ 可以直接调用

```lua
devServices = {
    Player = {
        name = "玩家模块",
        methods = {
            "GetHostUin",      -- ✅ 可以直接调用
            "OpenUIView",      -- ✅ 可以直接调用
            "PlayMusic",       -- ✅ 可以直接调用
        },
    },
}
```

规则 2：在 services 中但不在 devServices 中 → ⚠️ 可能需要特殊权限

```lua
services = {
    Player = {
        ix = 13,
        methods = {
            { "RefreshItemTips", 3 },          -- ⚠️ 不在 devServices 中
            { "OpenInnerItemTips", 3 },        -- ⚠️ 不在 devServices 中
        },
    },
}
```

规则 3：标记 LuaApi3_InternalApi → ❌ 权限不足

```lua
services = {
    Item = {
        methods = {
            {
                "EmpowerEquipOrGunForPlayer",   -- ❌ 内部 API
                0,
                {
                    [3] = "LuaApi3_InternalApi", -- 标记为内部 API
                },
            },
        },
    },
}
```

规则 4：在 dismethods 中 → ❌ 禁用，调用直接失败

```lua
services = {
    Player = {
        dismethods = {
            "GetActorByObjid",       -- ❌ 禁用
            "GetObjTypeByActor",     -- ❌ 禁用
            "ChangeCustomModelOld",  -- ❌ 禁用
        },
    },
}
```

---

三、快速查询表

位置 含义 能否调用
devServices 开发者可见 ✅ 可以
services（无标记） 引擎内部，未公开 ⚠️ 可能需要权限
services + LuaApi3_InternalApi 内部 API ❌ 权限不足
dismethods 禁用方法 ❌ 直接失败
ScriptFenvG 全局变量白名单 ✅ 白名单内可用

---

四、实战案例

案例 1：Player:RefreshItemTips

查询步骤：

1. 在 services.Player.methods 中找到：
   ```lua
   { "RefreshItemTips", 3 }
   ```
   → 存在，类型 Sync
2. 在 devServices.Player.methods 中查找：
   ```lua
   -- 没有找到 RefreshItemTips
   ```
   → 不在开发者可见列表中
3. 结论： ⚠️ 引擎内部接口，未完全公开，需要 Tips 系统初始化后才能调用。

---

案例 2：Item:EmpowerEquipOrGunForPlayer

查询步骤：

1. 在 services.Item.methods 中找到：
   ```lua
   {
       "EmpowerEquipOrGunForPlayer",
       0,
       {
           [3] = "LuaApi3_InternalApi",
       },
   }
   ```
   → 标记 LuaApi3_InternalApi
2. 结论： ❌ 内部 API，普通脚本调用会权限不足。

---

案例 3：Player:PlayMusic

查询步骤：

1. 在 services.Player.methods 中找到：
   ```lua
   { "PlayMusic", 3 }
   ```
   → 存在
2. 在 devServices.Player.methods 中查找：
   ```lua
   "PlayMusic",
   ```
   → 存在
3. 结论： ✅ 可以直接调用。

---

五、特殊情况

1. 有冷却限制的接口

```lua
{
    "PlayAdvertising",
    0,
    {
        [2] = {
            90,
            "调用频繁，请稍后尝试！",
        },
    },
},
```

含义： 90 秒内只能调用一次，超过会提示"调用频繁"。

2. 有参数限制的接口

```lua
{
    "SendFriendApply",
    3,
    {
        [2] = {
            10,
            "",
        },
    },
},
```

含义： 10 秒内只能调用一次。

3. 环境限制的接口

```lua
{
    name = "GetPosition",
    envType = 3,  -- Motion（运动环境）
},
```

含义： 该接口在特定环境下才可用。

---

六、常见标记含义

标记 含义
LuaApi3_InternalApi 内部 API，权限不足
trigger_api_xxx 触发器专用 API
envType = 3 运动环境（Motion）
[2] = { 秒数, 提示 } 冷却限制
[3] = "xxx" API 来源标记
[4] = 秒数 超时时间
[5] = { 3 } 参数限制
[6] = { 3, "xxx" } 互斥方法
[7] = 1 特殊标记

---

七、查询流程图

```
开始查询某个接口
    ↓
在 devServices 中查找
    ├── 找到 → ✅ 可以直接调用
    └── 没找到
        ↓
    在 services 中查找
        ├── 没找到 → ❌ 接口不存在
        └── 找到
            ↓
        检查是否有 LuaApi3_InternalApi 标记
            ├── 有 → ❌ 权限不足
            └── 没有
                ↓
            检查是否有 dismethods 标记
                ├── 有 → ❌ 禁用
                └── 没有
                    ↓
                检查是否有冷却限制
                    ├── 有 → ⚠️ 可以调用但有冷却
                    └── 没有 → ⚠️ 可以尝试调用（可能需要初始化）
```

---

八、实战示例

场景：判断 Player:GetFriendList 能否调用

```lua
-- 1. 在 devServices.Player.methods 中找到
"GetFriendList",

-- 2. 在 services.Player.methods 中找到
{
    "GetFriendList",
    5,
    {
        [2] = {
            10,
            "调用频繁，请稍后尝试！",
        },
    },
},

-- 结论：✅ 可以调用，但有 10 秒冷却
```

---

九、总结

判断依据 结论
devServices 中存在 ✅ 可以直接调用
services 中存在但 devServices 中不存在 ⚠️ 可能需要初始化
有 LuaApi3_InternalApi 标记 ❌ 权限不足
在 dismethods 中 ❌ 禁用
有冷却限制 [2] ⚠️ 有调用频率限制
ScriptFenvG 中存在 ✅ 全局变量可用

---

十、附录：常用标记速查

标记 值 含义
[2] {秒数, 提示} 冷却限制
[3] "LuaApi3_InternalApi" 内部 API
[4] 数字 超时时间（秒）
[5] {3} 参数限制
[6] {3, "xxx"} 互斥方法
[7] 1 特殊标记

---

Last updated: 2025

```